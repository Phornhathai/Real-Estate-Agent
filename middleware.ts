// =============================================================================
// 📁 middleware.ts — ป้องกัน /admin ให้เข้าได้เฉพาะคนที่ login แล้ว
// =============================================================================

export { auth as middleware } from "@/auth";

export const config = {
  matcher: ["/admin/:path*"],
};
